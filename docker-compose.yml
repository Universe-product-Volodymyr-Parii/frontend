services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: products-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
